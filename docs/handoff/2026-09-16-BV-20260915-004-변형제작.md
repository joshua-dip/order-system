# BV-20260915-004 변형문제 제작 인수인계 · 2026-09-16

## 주문 정보

- 주문번호: **BV-20260915-004** (장훈 선생님, hunni1283@naver.com, 상태 payment_confirmed)
- 교재: `수능유형 PICK 독해 실력 [2022 개정](2023.01)`
- 범위: 지문 39개 × 유형 11종(주제·제목·주장·일치·불일치·함의·빈칸·요약·어휘·순서·삽입) × 3문항 = **총 1,287문항**
- 유형당 목표(requiredPerType): 3

## 진행 현황 (2026-09-16 최종)

- ✅ **완료 39/39 — 1,287문항 전량 저장** (`needCreateGrandTotal: 0`)
- 1지문(c95)은 메인 세션에서 직접, 나머지 38지문은 7개 wave 병렬 에이전트(샤드 s02~s39, wave당 최대 6지문)로 제작
- 마감 검증: pipeline 자동 검수 1,287/1,287 완료(검수불일치 0·실패 0) → `cc:answer-seq check` 11유형 전부 117/117 → 교재 단위 `cc:audit` errorTotal 0·contentIntegrity 0·duplicateOptions 0·orderUnified skewed false
- 주문 상태는 payment_confirmed 그대로 — completed 변경은 미논의, 임의 변경 금지

## 세션 재개 절차 (이 순서대로)

1. `npm run cc:variant -- pipeline:BV-20260915-004` — 부족 집계 + 대기 자동 검수. 이전 세션 저장분이 자동 검수(완료/검수불일치)된다.
2. `variant_get_shortage`(또는 pipeline 출력)에서 다음 미작성 지문 확인.
3. 지문당 반복:
   - `variant_get_passage { passage_id }`로 원문 확보
   - **채팅에서 직접** 11유형 × 3문항 = 33문항 question_data 작성 (Pro-only, `variant_generate_draft`·`variant_generate_and_save` 등 Anthropic API **금지**)
   - `.variant-drafts/bv-20260915-004-p<NN>.json`에 배열로 저장. 각 항목에 `"ai_source": "kimi"` 포함 (Kimi가 작성한 문항은 ai_source 기록 필수 — HANDOFF.md 2026-09-16 규칙)
   - `npx tsx scripts/cc-variant-prevalidate.ts <파일>` → **ERROR 0 · warning 0** 확인
   - `npm run cc:variant -- save --json <파일>` — **딱 한 번만** 실행 (dry-run 없음, 재실행 시 중복 insert 사고 이력 있음)
4. 저장 후 확인은 `shortage`로만 (save가 든 셸 명령 재실행 금지).
5. 전 지문 완료 후: pipeline 재실행(재검수) → `npm run cc:answer-seq -- check BV-20260915-004` → 교재 단위 `cc:audit` → 결과 보고.

## 작성 규칙 레퍼런스

- 실전 교정 규칙: `docs/variant/AUTHORING.md` (유형별 형식·저장 주의사항 — 반드시 숙지)
- 권위 프롬프트: `lib/admin-variant-draft-claude.ts`, `lib/variant-draft-blank-summary-rules.ts`
- 핵심만 요약:
  - 주제·제목·주장·일치·불일치: Paragraph 원문 그대로(`<u>` 금지), 영어 보기, `###` 구분
  - 함의: `<u>표현</u>` + Question에 곧은 큰따옴표로 동일 표현 반복
  - 빈칸: `<u>_____</u>` 1곳만, 정답 표현이 본문에 남으면 안 됨
  - 요약: Paragraph = 본문 + `\n\n→ ` 요약문((A)/(B) 빈칸), 보기 `① (A) w - (B) w`
  - 어휘: 5개 낱말 ①~⑤ 표시 중 1개만 문맥상 부적절, Options `① word ### …` (번호 필수·단어만) — **비셔플 유형이라 정답 위치 직접 분산**
  - 순서: 주어진 문장 + (A)(B)(C) 블록(`\n###\n` 구분), Options 고정 5순열, 블록 첫 문장 원문 그대로 — 비셔플, 정답 분산
  - 삽입: 주어진 문장 + 빈 줄 + 본문(①~⑤ 마커 정확히 5개) — 비셔플, 정답 분산
  - Explanation: 한국어 450자 이하, `"② 가 정답입니다."`로 시작
  - option_type은 항상 `"English"`, status는 `"대기"`

## 주의

- 원문에 오타처럼 보이는 부분(예: `due. to`)도 DB 원문 그대로 유지 — 순서 정답 검증이 원문 위치 대조로 동작.
- 같은 (지문,유형) 3건의 정답이 셔플 후 우연히 같은 번호로 몰리면 `shuffleQuestionDataForDistribution`으로 updateOne 패치(재저장 아님).
- 검수불일치(forced)가 나오면 사유별 처리 표는 `scripts/cc-order-pipeline-prompt.md` §4 참고.
