'use client';

import { ClassKitLectureView } from './ClassKitLectureView';

/** Admin 강의용자료 라우트 — 기본값(admin API) 그대로. */
export default function ClassKitLecturePage() {
  return <ClassKitLectureView />;
}
