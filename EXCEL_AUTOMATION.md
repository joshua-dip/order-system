# 📊 엑셀 파일 자동 변환 시스템

이 시스템은 `assets` 폴더에 엑셀 파일을 업로드하면 자동으로 `app/data/converted_data.json`에 추가하는 기능을 제공합니다.

## 🚀 설치 방법

### 1. Python 패키지 설치

```bash
pip3 install -r requirements.txt
```

필요한 패키지:
- `pandas` - 엑셀 파일 읽기
- `openpyxl` - .xlsx 파일 지원
- `watchdog` - 파일 시스템 감시

## 📝 사용 방법

### 방법 1: 자동 감시 모드 (권장)

터미널에서 다음 명령을 실행하세요:

```bash
python3 scripts/watch_excel.py
```

이제 `assets` 폴더에 엑셀 파일을 추가하면 자동으로 변환됩니다!

```
📁 엑셀 파일 자동 변환 시스템 시작
📂 감시 폴더: /Users/goshua/next-order/assets
📝 변환 스크립트: /Users/goshua/next-order/scripts/excel_to_json.py

💡 사용법:
   1. assets 폴더에 엑셀 파일(.xlsx, .xls)을 추가하세요
   2. 자동으로 converted_data.json에 추가됩니다

⏳ 파일 변경을 감시 중... (Ctrl+C로 종료)
```

### 방법 2: 수동 변환

특정 엑셀 파일을 직접 변환하려면:

```bash
python3 scripts/excel_to_json.py assets/교재명.xlsx
```

## 📋 엑셀 파일 형식

엑셀 파일은 다음과 같은 형식이어야 합니다:

| 교재명 | 강/회차 | 번호 |
|--------|---------|------|
| 수능실감 실감하다 300제 (2023) | 01강 | 1번 |
| 수능실감 실감하다 300제 (2023) | 01강 | 2번 |
| 수능실감 실감하다 300제 (2023) | 01강 | 3번 |
| 수능실감 실감하다 300제 (2023) | 02강 | 1번 |
| ... | ... | ... |

### 중요 사항:
1. **첫 번째 열**: 교재명 (같은 교재는 병합 가능)
2. **두 번째 열**: 강 또는 회차 (예: "01강", "핵심독해 모의고사 01회")
3. **세 번째 열**: 문제 번호 (예: "1번", "18~19번")
4. **파일명**: 파일명이 최상위 교재 키로 사용됩니다 (예: `수능실감 실감하다 300제 (2023).xlsx`)

## 🔄 변환 결과

엑셀 파일이 다음과 같은 JSON 구조로 변환됩니다:

```json
{
  "수능실감 실감하다 300제 (2023)": {
    "Sheet1": {
      "부교재": {
        "수능실감 실감하다 300제 (2023)": {
          "01강": [
            { "번호": "1번" },
            { "번호": "2번" },
            { "번호": "3번" }
          ],
          "02강": [
            { "번호": "1번" },
            { "번호": "2번" }
          ]
        }
      }
    }
  }
}
```

## ⚠️ 주의사항

1. **기존 데이터 보존**: 같은 교재명이 이미 있으면 덮어씁니다.
2. **파일명 중요**: 엑셀 파일명이 교재 키로 사용됩니다.
3. **임시 파일 제외**: `~$`로 시작하는 엑셀 임시 파일은 자동으로 무시됩니다.
4. **JSON 검증**: 변환 후 자동으로 JSON 유효성이 검증됩니다.

## 🛠️ 문제 해결

### 패키지 설치 오류
```bash
# pip 업그레이드
pip3 install --upgrade pip

# 패키지 재설치
pip3 install -r requirements.txt --force-reinstall
```

### 권한 오류
```bash
chmod +x scripts/excel_to_json.py
chmod +x scripts/watch_excel.py
```

### JSON 형식 오류 확인
```bash
python3 -m json.tool app/data/converted_data.json
```

## 📁 폴더 구조

```
next-order/
├── assets/                    # 엑셀 파일 업로드 폴더
│   ├── 교재1.xlsx
│   └── 교재2.xlsx
├── scripts/                   # 자동화 스크립트
│   ├── excel_to_json.py      # 엑셀 → JSON 변환
│   └── watch_excel.py        # 파일 감시 스크립트
├── app/
│   └── data/
│       └── converted_data.json  # 변환된 데이터
└── requirements.txt           # Python 패키지
```

## 💡 팁

1. **백그라운드 실행**: 
   ```bash
   nohup python3 scripts/watch_excel.py > watch.log 2>&1 &
   ```

2. **여러 파일 한번에 변환**:
   ```bash
   for file in assets/*.xlsx; do
     python3 scripts/excel_to_json.py "$file"
   done
   ```

3. **Git에서 제외**: `.gitignore`에 `assets/` 추가하여 엑셀 원본 파일은 커밋하지 않기

